# Docker Compose Environment Variables
# Copy this file to .env and fill in your values

# Docker Hub Configuration (for pulling remote images)
# Leave empty to build locally instead
DOCKER_USERNAME=

# Port Configuration
BACKEND_PORT=5000
FRONTEND_PORT=80

# Backend URL for frontend (adjust for production deployments)
BACKEND_URL=http://localhost:5000
